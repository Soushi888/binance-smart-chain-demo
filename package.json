{
  "name": "binance-smart-chain-test",
  "private": true,
  "version": "0.0.0",
  "type": "commonjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "hardhat:local": "hardhat node",
    "contracts:compile": "hardhat compile",
    "contracts:deploy:localhost": "hardhat run --network localhost scripts/deploy.js",
    "contracts:deploy:testnet": "hardhat run --network testnet scripts/deploy.js"
  },
  "devDependencies": {
    "vite": "^4.0.0"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.1",
    "hardhat": "^2.12.6"
  }
}
